<!-- resume.jinja -->
<article class="resume" data-page="resume">

  <header>
    <h2 class="h2 article-title">Resume</h2>
  </header>

  <section class="timeline">

    <div class="title-wrapper">
      <div class="icon-box">
        <ion-icon name="book-outline"></ion-icon>
      </div>

      <h3 class="h3">Experience</h3>
    </div>

    <ol class="timeline-list">

      {% for experience in resume.experience %}

      <li class="timeline-item">

        <h4 class="h4 timeline-item-title">{{experience.title}}</h4>

        <span>{{experience.timeline}}</span>

        <p class="timeline-text">
          {{experience.description}}
        </p>


      </li>

      {% endfor %}

    </ol>

  </section>

    <section class="timeline">

    <div class="title-wrapper">
      <div class="icon-box">
        <ion-icon name="book-outline"></ion-icon>
      </div>

      <h3 class="h3">Education</h3>
    </div>

    <ol class="timeline-list">

      {% for education in resume.education | reverse %}

      <li class="timeline-item">

        <h4 class="h4 timeline-item-title">{{education.title}}</h4>

        <span>{{education.timeline}}</span>

        <p class="timeline-text">
          {{education.description}}
        </p>

      </li>

      {% endfor %}

    </ol>

  </section>

  <!-- Languages -->
  {% if resume.languages.enabled %}
  <section class="skill">

    <h3 class="h3 skills-title">{{resume.languages.title}}</h3>

    <ul class="skills-list content-card">

      {% for language in resume.language %}

      <li class="skills-item">
        <div class="title-wrapper">
          <h5 class="h5">{{language.title}}</h5>
          {% if resume.languages.show_value %}
          <data value="{{language.value[:-1]}}">{{language.value}}</data>
          {% endif %}
        </div>
        {% if resume.languages.show_bar %}
        <div class="skill-progress-bg">
          <div class="skill-progress-fill" style="width: {{language.value}};"></div>
        </div>
        {% endif %}
      </li>

      {% endfor %}

    </ul>

  </section>
  <br>
  {% endif %}


  <!-- My Skills -->

  {% if resume.skills.enabled %}
  <section class="skill">

    <h3 class="h3 skills-title">{{resume.skills.title}}</h3>

    <ul class="skills-list content-card">

      {% for skill in resume.skill %}

      <li class="skills-item">
        <div class="title-wrapper">
          <h5 class="h5">{{skill.title}}</h5>
          {% if resume.skills.show_value %}
          <data value="{{skill.value[:-1]}}">{{skill.value}}</data>
          {% endif %}
        </div>
        {% if resume.skills.show_bar %}
        <div class="skill-progress-bg">
          <div class="skill-progress-fill" style="width: {{skill.value}};"></div>
        </div>
        {% endif %}
      </li>

      {% endfor %}

    </ul>

  </section>
  {% endif %}



<section class="language">
    <h3 class="h3 skills-title">{{ languages.title }}</h3>

    <h4>Básico</h4>
    <ul class="skills-list content-card">
        {% for lang in languages.language %}
            {% if lang.value == "A1" or lang.value == "A2" %}
                <li class="skills-item">
                    <div class="title-wrapper">
                        <h5 class="h5">{{ lang.title }}</h5>
                        {% if lang.description %}
                            <p>{{ lang.description }}</p>
                        {% endif %}
                        <span class="level">{{ lang.value }}</span>
                    </div>
                </li>
            {% endif %}
        {% endfor %}
    </ul>

    <h4>Intermedio</h4>
    <ul class="skills-list content-card">
        {% for lang in languages.language %}
            {% if lang.value == "B1" or lang.value == "B2" %}
                <li class="skills-item">
                    <div class="title-wrapper">
                        <h5 class="h5">{{ lang.title }}</h5>
                        {% if lang.description %}
                            <p>{{ lang.description }}</p>
                        {% endif %}
                        <span class="level">{{ lang.value }}</span>
                    </div>
                </li>
            {% endif %}
        {% endfor %}
    </ul>

    <h4>Avanzado</h4>
    <ul class="skills-list content-card">
        {% for lang in languages.language %}
            {% if lang.value == "C1" or lang.value == "C2" %}
                <li class="skills-item">
                    <div class="title-wrapper">
                        <h5 class="h5">{{ lang.title }}</h5>
                        {% if lang.description %}
                            <p>{{ lang.description }}</p>
                        {% endif %}
                        <span class="level">{{ lang.value }}</span>
                    </div>
                </li>
            {% endif %}
        {% endfor %}
    </ul>
</section>




</article>